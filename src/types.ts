export type Rect = {
    width: number
    height: number
    left: number,
    top: number,
    borderColor?: string,
    id?: string,
}
export type Transform = {
    x: number
    y: number
    k: number
}
